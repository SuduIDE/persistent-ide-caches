package caches;

import caches.changes.AddChange;
import caches.records.FilePointer;
import caches.records.Trigram;
import caches.trigram.TrigramCache;
import caches.trigram.TrigramHistoryIndex;
import caches.trigram.TrigramNode;

import java.io.File;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.io.RandomAccessFile;

public class Main {
    public static void main(String[] args) {
//        var file = new File("/home/alex/Programming/caches/test-indexes-repo/file1");
//        GlobalVariables.filesInProject.put(1, file);
//        GlobalVariables.reverseFilesInProject.put(file, 1);
//        var index = new TrigramHistoryIndex();
//        index.processChange(new AddChange(new FilePointer(file, 0),
//                System.currentTimeMillis(),
//                "text"));
//        try (var raf = new RandomAccessFile(new File(TrigramHistoryIndex.DIRECTORY + "ext"), "r")) {
//            var tmp = TrigramNode.readTrigramNode(raf);
//            System.out.println(tmp);
//        }

    }
}
